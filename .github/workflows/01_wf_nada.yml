name:  01_wf_nada
 on:
  push:
    branches:
      - main
      - develop

jobs:
  example_job:
    runs-on: ubuntu-latest

    steps:  
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Execute a command
      run: echo "Hello, GitHub Workflow!"

