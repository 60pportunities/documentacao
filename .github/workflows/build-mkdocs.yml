ame: Print Secrets

on:
  push:
    branches:
      - main

jobs:
  print-secrets:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Print secrets
      run: 
        | echo "TOKEN : ${{ secrets.TOKEN }}"
