ame: Print Secrets

on:
  push:
    branches:
      - main

jobs:
  print-secrets:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2
      
  - shell: bash
    env:
      SUPER_SECRET: ${{ secrets.TOKEN }}
    run: |
      echo  "$SUPER_SECRET"
